# Cross-repo hygiene register (#1–#9) — consolidated 2026-09-04

> **What this is.** A cross-repo "punch list" that was tracked in Claude
> sessions rather than in any repo tracker, worked through across several
> sessions in late Aug / early Sep 2026, and driven to closure on 2026-09-03.
> It is an **infrastructure / hygiene** list (blueprint + vortex + testchat),
> distinct from the product roadmap in `tasks/TODO.md` and `tasks/BACKLOG.md`.
> Recorded here so it survives outside chat scrollback ("backlog queue empty"
> — it was never written to a file).
>
> **Fidelity note.** Items #7, #8, #9 are quoted verbatim from the thread.
> Items #1, #2, #4, #5 are **reconstructed from their closeout decision
> references** — the original one-line problem statements were not recovered
> from the transcript; only their outcomes and decision numbers are known.
> Backfill the verbatim wording if the original source list resurfaces.
>
> **Ground-truth at closeout (verified live in-session, not relayed):**
> blueprint `1ea7f87`, testchat `ae5d8e2`, vortex `3b2a7e4` — all HEAD = origin,
> CI + check-drift green. (Both children advanced afterward: testchat to
> `c6264ff` with spec v122; vortex to `24fcd94` with the anneal fix committed.)

---

## The register

| # | Item | Severity | Status | Evidence |
|---|------|----------|--------|----------|
| 1 | Blueprint/child hardening (reconstructed — e.g. `validate-plan.py` family-match + child re-pin correctness) | — | ✅ Done & verified | D-178 |
| 2 | Blueprint/child hardening (reconstructed — original statement not recovered) | — | ✅ Done & verified | D-179 |
| 3 | Error-copy + verbatim-brief authoring — **two halves, two repos** | Med | ⚠️ Testchat half **shipped**; vortex half **still open** | see below |
| 4 | Blueprint/child hardening (reconstructed — original statement not recovered) | — | ✅ Done & verified | D-180 / D-181 |
| 5 | Hardening item (reconstructed — original statement not recovered) | — | ✅ **CEO-validated** | D-182 |
| 6 | Silent-halt / two-strike escalation-ladder live-fire | — | ✅ **Live-proven** | v115 organic run (2026-09-02); closes vortex TODO **T6** |
| 7 | CLAUDE.md oversized (~84 KB) — decision log in the hot path | Medium | ✅ Done | log split → `docs/CORRECTION-LOG.md` + pointer; `b6626b0` |
| 8 | Distribution model: "born-linked" seed-path simplification | Medium | ✅ Done | `new-project.sh --linked` — **D-183**, `a87f014` |
| 9 | Coder-brief guidance too weak (blueprint half — prose/lints don't force exact object/method/imports) | Med–High | ✅ Done | Rule 8 clause + `.opencode/prompts/em-plan.md` |

Also landed in the same arc: **D-74** (diff-scope the coder-output lint gate —
`scripts/lint-changed.py` + its selftest, synced into both children).

Closeout line, verified live: *"#1–#7 and #9 done and verified (D-178…D-182,
D-74, hot-path split, Rule 8 clause); #5 CEO-validated; #6 live-proven; #3
closed as design limitation; backlog queue empty."*

---

## Item #3 — the two halves (the one with an open remainder)

- **Testchat half — chat error copy.** The 404-race path showed a *suggestion*
  ("Model … is not ready in Vortex. Pick a local model or retry once it is
  loaded."). CEO decision refined this to **one generic retry line for every
  failure, no suggestions**. Shipped as **spec v122** (AC-182 supersedes AC-179):
  `_messageless_error()` in `src/api/chat.py` returns the generic `FALLBACK_REPLY`;
  the "pick a local model" suggestion is gone (doubly obsolete now that Vortex
  owns model management). 34 router+chat tests green; ruff/mypy clean; CI green
  on `c6264ff`. The earlier distinct-per-cause design
  (`testchat/tasks/llm-error-copy-design.md`) was **not built** — retained only
  as provenance for the superseded approach.
- **Vortex half — "verbatim-brief authoring." ⛔ STILL OPEN.** Never specified
  concretely enough to act on; the grep only surfaces the existing verbatim-brief
  machinery in the role docs. **This is the single register item that genuinely
  needs a CEO scope call before it is actionable.**

---

## Residuals still open (per the session's own closeout — none blocking)

1. **Vortex anneal-lane WIP** — `config/catalog.json` + `src/vortex/lifecycle.py`,
   an uncommitted fix owned by a *different* lane at closeout. (Appears committed
   as of vortex `24fcd94`; worth a 30-second confirm it wasn't orphaned.)
2. **#3 vortex half — "verbatim-brief authoring"** — see above; needs a CEO
   scope call.
3. **Vestigial testchat catalog UI** (forward note, no action asked): with Vortex
   owning load/unload, testchat's own load/unload/Eject buttons are wired to the
   now-frozen local loader. A later product call on whether to hide that UI.

---

## Related measurement/builder track (parked, tracked in `tasks/TODO.md`)

Worked in the same arc but recorded in the combined `tasks/TODO.md`, not this
register. Captured here only as a cross-reference:

- **2b / 2b-ext mutation sweeps** — done (45/68 killed; 23 survivors = the
  oracle-gap map). **Group 1–3 oracle-gap fixes done; `check-test-surface.py`
  mutation proof done.**
- **Catch ledger, tiering + cost accounting, vortex recording hook** — done.
- Standing (recurring): post-blueprint-landing verification after any
  linked-file change; periodic `gate-tiering.py` + catch-ledger review.

---

## Provenance (source Claude sessions)

- `local_77396557` ("Untitled session", active 2026-09-04) — drove #3 (testchat),
  #8 verification, and the final live-verified closeout.
- `local_37c56b3d` ("SW Dev Blueprint system", 2026-08-26) — the measurement/
  builder track and earlier "what's pending" snapshots.
- The full original #1–#9 list was pasted into `local_77396557` from an external
  source; only the #7–#9 tail is preserved verbatim in that transcript.
